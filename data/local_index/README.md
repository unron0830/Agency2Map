# Local RAG Index

This folder is reserved for local-only retrieval indexes built from private or mixed-permission documents.

Do not commit generated vector indexes, chunks from non-public documents, or embeddings derived from restricted text. Publicly share only reviewed outputs such as `sample_data/resource_items.csv`, `sample_output/resource_items.geojson`, or source citations that are lawful to disclose.

